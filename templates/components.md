# 组件模板库

## 卡片组件
- **组件名称**: [必填] 例如：用户信息卡片
- **卡片类型**: [必填] 例如：基础卡片、带图片卡片、带操作卡片
- **尺寸**: [必填] 例如：宽300px，高200px
- **边框样式**: [必填] 例如：圆角8px，阴影0 2px 4px rgba(0,0,0,0.1)
- **内容布局**: [必填] 例如：标题在上，内容在中，操作在下
- **交互效果**: [必填] 例如：悬停时阴影加深、轻微放大
- **空态样式**: [必填] 例如：无内容时显示占位符和提示文字

## 按钮组件
- **按钮名称**: [必填] 例如：提交按钮
- **按钮类型**: [必填] 例如：主按钮、次要按钮、危险按钮、链接按钮
- **尺寸**: [必填] 例如：宽100px，高40px
- **文字样式**: [必填] 例如：粗体，14px
- **背景样式**: [必填] 例如：主色调背景，白色文字
- **边框样式**: [必填] 例如：无边框，圆角4px
- **交互效果**: [必填] 例如：悬停时背景色加深、点击时轻微下沉
- **禁用状态**: [必填] 例如：半透明背景，不可点击

## 表单组件
- **表单名称**: [必填] 例如：用户注册表单
- **字段列表**: [必填] 例如：
  - 用户名：文本输入，必填，最大长度20
  - 密码：密码输入，必填，8-16位
  - 邮箱：邮箱输入，必填，验证格式
- **布局方式**: [必填] 例如：垂直排列，标签在上，输入框在下
- **验证规则**: [必填] 例如：实时验证、提交时验证
- **错误提示**: [必填] 例如：红色文字，显示在输入框下方

## 列表组件
- **列表名称**: [必填] 例如：用户列表
- **列表类型**: [必填] 例如：普通列表、表格列表、卡片列表
- **数据字段**: [必填] 例如：ID、名称、状态、操作
- **排序功能**: [必填] 例如：支持按字段排序、升降序
- **筛选功能**: [必填] 例如：支持按状态筛选、搜索筛选
- **分页功能**: [必填] 例如：每页显示10条，支持页码跳转
- **操作功能**: [必填] 例如：查看、编辑、删除

## 弹窗组件
- **弹窗名称**: [必填] 例如：确认弹窗
- **弹窗类型**: [必填] 例如：确认弹窗、信息弹窗、表单弹窗
- **尺寸**: [必填] 例如：宽400px，高300px
- **标题**: [必填] 例如：确认操作
- **内容**: [必填] 例如：您确定要删除这条记录吗？
- **按钮**: [必填] 例如：取消按钮、确认按钮
- **背景遮罩**: [必填] 例如：半透明黑色背景
- **关闭方式**: [必填] 例如：点击关闭按钮、点击遮罩、按ESC键

## 导航组件
- **导航名称**: [必填] 例如：顶部导航
- **导航类型**: [必填] 例如：顶部导航、左侧导航、面包屑导航
- **菜单项**: [必填] 例如：首页、用户管理、设置
- **激活状态**: [必填] 例如：当前页面菜单项高亮显示
- **下拉菜单**: [可选] 例如：二级菜单、三级菜单
- **移动端适配**: [必填] 例如：折叠为汉堡菜单

### 面包屑导航（多页面一致性关键组件）
多页面原型中，所有页面的面包屑导航**必须**使用统一的 HTML 结构和 CSS 样式：

**统一 HTML 结构**:
```html
<nav class="breadcrumb" aria-label="breadcrumb">
  <a href="#" class="breadcrumb-link">首页</a>
  <span class="breadcrumb-sep">/</span>
  <a href="#" class="breadcrumb-link">父级页面</a>
  <span class="breadcrumb-sep">/</span>
  <span class="breadcrumb-current">当前页面</span>
</nav>
```

**统一 CSS 规范**:
- 字号: 14px
- 链接颜色: #666，悬停时变为主色
- 当前页文字: #333，font-weight: 600
- 分隔符: "/"，颜色 #ccc，两侧间距 4px~8px
- 面包屑整体间距: margin-bottom 16px
- 样式只定义一次（全局 CSS），禁止在每个页面中重复定义

## 图表组件
- **图表名称**: [必填] 例如：用户增长图表
- **图表类型**: [必填] 例如：折线图、柱状图、饼图
- **数据来源**: [必填] 例如：用户注册数据
- **数据展示**: [必填] 例如：按月份展示，显示数值标签
- **交互功能**: [必填] 例如：鼠标悬停显示详细数据、支持缩放

## 标签组件
- **标签名称**: [必填] 例如：状态标签
- **标签类型**: [必填] 例如：状态标签、分类标签
- **颜色**: [必填] 例如：成功（绿色）、警告（黄色）、危险（红色）
- **尺寸**: [必填] 例如：高度20px，圆角10px
- **文字样式**: [必填] 例如：12px，白色文字

## 进度条组件
- **进度条名称**: [必填] 例如：任务进度条
- **类型**: [必填] 例如：线性进度条、环形进度条
- **尺寸**: [必填] 例如：宽度100%，高度8px
- **颜色**: [必填] 例如：主色调
- **动画效果**: [必填] 例如：加载时的动画效果
- **数值显示**: [可选] 例如：显示百分比数值